# 同花顺 Harness 终端改进计划

> 基线：DSH `0.1.1-rc.1`，已提交兼容性修复。
> 目标：把当前“股票终端外壳 + 部分模拟数据”逐步改造成 **真实 Harness 数据驱动的可交互终端**，同时保留同花顺式行情/盘口/K线体验。

## 0. 现状总结

- 已有完整三栏终端布局：顶部指数条、左栏导航/自选、中栏对话+K线、右栏行情/变更/流向、底部状态栏。
- 已接入真实 DSH 会话：
  - 顶部会话切换、发送/停止/新建会话；
  - 对话、Trajectory 使用真实会话节点；
  - `/tonghuashun/snapshot` 数据接口可用。
- 仍是模拟/装饰的部分：
  - 左栏“关注项目”默认是模拟工作区；
  - 右栏行情、最近变更、git tree、token 流向、分时成交多数仍为模拟；
  - K 线无缩放/平移/指标/画线/叠加/全屏；
  - 导航（技能/插件/设置）、模型/权限/附件、检查点等未接通；
  - 大量按钮“徒有其表”。

## 1. 改进原则

1. 优先接真实数据，其次补交互，最后做股票软件专业功能。
2. 所有新增能力必须保持 DSH `0.1.1-rc.1` 兼容，构建/typecheck/smoke/单元测试通过。
3. 不破坏当前“可启动、可看、可聊天”的基线。
4. 无法真实接入的功能，先降级为明确提示或隐藏，避免“点了没反应”。

## 2. 阶段与任务

### Phase 1：真实数据接入（P0）

- [x] 1.1 左栏关注项目改为真实 workspace 列表
  - 使用 `useWorkspaces` / `useSessions` 提供真实工作区；
  - 结合 meter `snapshot.workspaces` 显示 Token/会话/工具调用；
  - 真实基线就绪且无工作区时显示空态。
- [x] 1.2 右栏行情改为真实选中工作区数据
  - 当前选中 workspace 的今日/累计 Token、会话数、工具调用数；
  - 最近变更、git tree、token 流向优先使用真实 snapshot。
- [x] 1.3 K 线数据源真实化
  - 日K 使用 `snapshot.daySeries`（有 live 快照时）；
  - 分时使用 `snapshot.minuteSeries`（有 live 快照时）；
  - 无真实数据时显示空态/降级提示。
- [x] 1.4 搜索真实化
  - 搜索范围扩展：真实会话、工作区、模型、文件/路径（已完成真实会话+工作区；模型/文件待补）；
  - 支持打开会话/切换工作区。

### Phase 2：真实 DSH 操作接通（P0）

- [ ] 2.1 左侧导航（技能/插件/设置）接通 DSH 窗口或命令
  - 能打开 DSH 技能、插件、设置界面；
  - 或至少通过命令面板/URL 路由进入。
- [x] 2.2 Composer 模型选择
  - 点击模型 chip 打开模型切换弹窗（命令式 `/model <name>`，真实模型列表待补）；
  - 发送 `/model <name>` 或调用 session 模型切换能力。
- [x] 2.3 权限模式选择
  - 显示当前 permission mode（当前未显示实际值）；
  - 点击切换 permission presets（命令式 `/permission <name>`）。
- [x] 2.4 附件接入
  - “＋”按钮打开文件选择器，插入 `@file:name` 标记（已可用）；
  - 真实 DSH 附件上传待补。
- [x] 2.5 检查点页签真实化
  - 从 session 日志读取 `compaction/summary`；
  - 展示检查点时间、摘要（可回退/继续待补）。

### Phase 3：股票软件专业图表（P1）

- [x] 3.1 K 线缩放/平移/复位
  - 滚轮缩放、拖拽平移、双击复位；
  - 十字光标点击固定并显示详情（点击固定待补）。
- [ ] 3.2 指标系统
  - MACD、KDJ、BOLL 等常用指标；
  - 可切换/配置 MA 参数。
- [ ] 3.3 叠加对比
  - 叠加另一个 workspace/模型 Token 曲线；
  - 叠加分时/日K。
- [ ] 3.4 画线工具
  - 趋势线、水平线、垂直线、标注；
  - 持久化到 localStorage。
- [x] 3.5 全屏
  - 真正的浏览器全屏 API；
  - 退出全屏恢复。
- [x] 3.6 “更多 / 前复权 / 叠加 / 画线 / 指标”菜单
  - 已提供点击反馈并明确标注“即将支持”；
  - 后续可替换为真实菜单。

### Phase 4：盘口 / 成交 / 流向（P1）

- [x] 4.1 五档盘口 = Harness 队列/工具/子代理
  - 已新增“盘口”页签，展示运行状态/会话/工具调用/关联会话；
  - pending queue、子代理、jobs 待补。
- [x] 4.2 分时成交真实化
  - 每分钟 Token 消耗来自真实 snapshot（有 live 数据时）；
  - 点击成交行跳转 Trajectory 页签（已完成）。
- [x] 4.3 token 流向细化
  - 按模型拆分；
  - 展示 input/output/cacheRead/cacheWrite/reasoning 五类（今日明细）。
- [x] 4.4 最近变更/git tree 可点击
  - 点击打开文件/diff（已完成系统打开路径；diff 待补）；
  - 点击 commit 查看详情（待补）。

### Phase 5：快捷键 / 搜索 / 自选 / 体验（P2）

- [x] 5.1 快捷键
  - `/` 聚焦 composer（已完成）；
  - `Ctrl+K` 全局搜索（已完成）；
  - `1~5` 切换 K 线周期（已完成）；
  - `Esc` 关闭浮层（已完成）；
  - `F11` 全屏（已完成）。
- [x] 5.2 自选持久化
  - 添加/移除关注项目；
  - localStorage 持久化。
- [x] 5.3 折叠/置顶/隐藏
  - 左栏折叠（已完成）；
  - 右栏置顶（仅图标，待补）；
  - 分时成交显示/隐藏可逆（已完成）。
- [x] 5.4 空态与加载态
  - 无工作区/真实数据等待时的明确空态（已完成）；
  - 加载骨架（待补）。
- [x] 5.5 响应式与无障碍
  - 窄屏布局（已完成基础断点）；
  - 键盘导航、aria 标签（已完成基础）。

## 3. 验收标准

- `npm run typecheck`、`npm run build`、`npm run smoke:bundle`、`npm test` 全部通过。
- DSH `0.1.1-rc.1` 启动后终端可渲染，无 console error / page error。
- 打开真实会话后，对话/Trajectory 正常。
- 每个“可点击”控件都有实际行为；暂未实现的不再伪装成可用按钮。

## 4. 里程碑

- M1：Phase 1 完成——界面数据以真实 DSH 为主，模拟仅作为降级。
- M2：Phase 2 完成——核心 DSH 操作可从终端直接执行。
- M3：Phase 3-4 完成——图表与盘口具备股票软件级交互。
- M4：Phase 5 完成——快捷键、持久化、响应式与整体打磨。
